{
  "scripts": {
    "install": "cd client-main && npm install && cd ../server && npm install",
    "build": "cd client-main && npm run build",
    "start": "cd server && node index.js"

  },
  "dependencies": {
    "ogl": "^1.0.11"
  }
}
