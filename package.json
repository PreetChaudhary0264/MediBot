{
  "scripts": {
    "install": "cd client-main && npm install && cd ../server && npm install",
     "build": "cd client-main && npm install && npm run build",
     "start": "cd server && npm install && node index.js"

  },
  "dependencies": {
    "ogl": "^1.0.11"
  }
}
